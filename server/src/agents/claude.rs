//! DEPRECATED: Claude NDJSON helpers have moved to `crate::llm::providers::claude`.
//! This module is kept empty to avoid breaking `mod` declarations during transition.
//! It will be removed in Gate 6 (relay cleanup).
